# Examples

Same `Kokkos::` math spelling; change the real and complex aliases.
`Kokkos::complex<DoubleDouble>` does not instantiate; see the header comment
in `type_swap_kernel.cpp` and `docs/COMPLEX_INTEROP.md`.
